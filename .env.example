# Required: set this to your real hostname (DNS record pointing at this server)
# Example: feed.example.com
FEEDLAND_DOMAIN=feedland.example.com

# Optional: enable Caddy reverse proxy / HTTPS later by uncommenting:
#COMPOSE_PROFILES=caddy

# MySQL credentials
# Option A: edit these manually
# Option B: generate them using ./scripts/generate-env.sh
MYSQL_ROOT_PASSWORD=change_me
MYSQL_USER_PASSWORD=change_me_too
